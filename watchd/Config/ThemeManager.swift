// ThemeManager wurde entfernt — Velvet Hour ist das einzige Theme der App.
// Das Theme wird statisch am App-Root via .environment(\.theme, .velvetHour)
// injiziert. Kein Switching, kein @AppStorage, kein Overhead.
